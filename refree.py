from board import Board
from game import Checkers

class Refree:
    def __init(self):
        self.role = Board(self)
        check_list = role.pos_set [:]
        target_set = []
    
    def _判断是否胜利(self):
        """如果所有10个格子都被占了，就返回true，否则就返回false"""
        for x, y  in self.target_set:
            if self.role.位置是否被占(x, y):
                continue
            else:
                
            break
